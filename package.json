{
  "name": "pastely",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix pastely",
    "build": "npm run build --prefix pastely",
    "preview": "npm run preview --prefix pastely"
  }
}
